import { FormatterMoneyPipe } from './formatter-money.pipe';

describe('FormatterMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatterMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
